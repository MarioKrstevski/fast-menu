import MenuDisplay from "../../../components/MenuDisplay/MenuDisplay";

export default function WebsitePreview(props) {
  return (
    <div className="h-full min-w-[60%] overflow-hidden">
      <MenuDisplay />
    </div>
  );
}
